import React from 'react';

const GeneralTab = (props) => {
    const {
        attributes,
        setAttributes,
        className,
        apiConfig,
        reducers
    } = props;

    return (
        <div>

        </div>
    );
};

export default GeneralTab;
